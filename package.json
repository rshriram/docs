{
  "name": "amalgam8",
  "version": "0.1.0",
  "description": "documentation",
  "homepage": "https://amalgam8.io",
  "license": "Apache-2.0",
  "scripts": {
    "docs:prepare": "gitbook install",
    "docs:watch": "npm run docs:prepare && gitbook serve",
    "docs:build": "npm run docs:prepare && rm -rf _book && gitbook build && cp -r _book/* ."
  },
  "devDependencies": {
    "gitbook-cli": "^2.3.0"
  }
}
