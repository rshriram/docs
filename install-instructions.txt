1. npm install gitbook-cli --save-dev
2. create content files under content/ folder
3. npm run docs:watch to view the content locally (content will be served out of _book folder)
4. npm run docs:build to build html files and copy them to main folder
5. Commit any html files that were modified or created by the previous command
6. push to github (gh-pages branch)

